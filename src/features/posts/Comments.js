const Comments = ({ comments }) => {

    const commentsElements = comments.map(comment => {
        return (
            <></>
        )
    })

    return (
        <>
            {commentsElements}
        </>
    )

}

export default Comments